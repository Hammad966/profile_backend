<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <div class="flex w-full justify-end">
        <a
          href="/logout"
          class="bg-red-500 rounded-lg px-4 py-2 text-sm mb-5 inline-block hover:bg-red-600 cursor-pointer"
          >Logout</a
        >
      </div>

      <h4 class="mb-5">Edit you post.</h4>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          placeholder="What's on your mind?"
          class="w-1/3 p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-lg text-gray-300"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          type="submit"
          class="px-4 py-2 bg-yellow-500 block w-40 rounded-lg mt-4 text-md cursor-pointer hover:bg-yellow-600 text-black"
          value="Update post"
        />
      </form>
    </div>
  </body>
</html>
